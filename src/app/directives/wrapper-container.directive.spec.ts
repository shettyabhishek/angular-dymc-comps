import { WrapperContainerDirective } from './wrapper-container.directive';

describe('WrapperContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new WrapperContainerDirective();
    expect(directive).toBeTruthy();
  });
});
