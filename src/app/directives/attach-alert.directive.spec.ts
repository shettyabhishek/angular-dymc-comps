import { AttachAlertDirective } from './attach-alert.directive';

describe('AttachAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachAlertDirective();
    expect(directive).toBeTruthy();
  });
});
